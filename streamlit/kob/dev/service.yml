---
apiVersion: v1
kind: Service
metadata:
  name: {APP_NAME}-service
  namespace: {KUBE_KOB_NAMESPACE}
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - port: 80
      targetPort: 8501
      name: http
  selector:
    app: {APP_NAME}
    version: "{route_version}"